"flex_vox_press" contains the 20 pressure values that the elastic tensors are measured at.

"n_list_flex" contains the 5 values of N that the elastic tensors are measured for

"flex_vox_cmat" contains 100 elastic tensors as a function of N and p. The reshaped array size is 5x100x20x6x6 (indexing system size, configuration, and pressure respectively)
